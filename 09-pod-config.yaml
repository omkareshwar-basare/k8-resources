apiVersion: v1
kind: Pod
metadata:
  name: pod-configmap-demo
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
    envFrom:
    - configMapRef:
        name: nginx-configmap