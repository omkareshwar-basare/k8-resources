apiVersion: v1
kind: Pod
metadata:
  name: env-demo
  namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
    env:
    - name: ENVIRONMENT
      value: dev    
    - name: project
      value: roboshop